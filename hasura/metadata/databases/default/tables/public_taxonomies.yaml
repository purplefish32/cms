table:
  schema: public
  name: taxonomies
is_enum: true
array_relationships:
  - name: term_taxonomies
    using:
      foreign_key_constraint_on:
        column: taxonomy
        table:
          schema: public
          name: term_taxonomies
insert_permissions:
  - role: user
    permission:
      check: {}
      columns:
        - taxonomy
select_permissions:
  - role: user
    permission:
      columns:
        - taxonomy
      filter: {}
      allow_aggregations: true
update_permissions:
  - role: user
    permission:
      columns:
        - taxonomy
      filter: {}
      check: {}
delete_permissions:
  - role: user
    permission:
      backend_only: false
      filter: {}
