query PagesTable {
  posts(where: { type: { _eq: page } }) {
    __typename
    id
    title
    slug
    body
    status
  }
  posts_aggregate(where: { type: { _eq: page } }) {
    __typename
    aggregate {
      count
    }
  }
}
