query PostsTable {
  posts(where: { type: { _eq: post } }) {
    id
    title
    slug
    body
    author_id
  }
  posts_aggregate(where: { type: { _eq: post } }) {
    aggregate {
      count
    }
  }
}
