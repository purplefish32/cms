query TagsTable {
  termTaxonomies(where: { taxonomy: { _eq: tags } }) {
    __typename
    taxonomy
    description
    termSlug
    term {
      __typename
      name
      slug
    }
    termRelationships_aggregate {
      __typename
      aggregate {
        count
      }
    }
  }
}
