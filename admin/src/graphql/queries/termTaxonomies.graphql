query termTaxonomies($where: termTaxonomies_bool_exp) {
  termTaxonomies(where: $where) {
    __typename
    taxonomy
    description
    termSlug
    parentSlug
    term {
      __typename
      name
      slug
    }
  }
}
