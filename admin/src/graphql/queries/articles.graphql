query articles($where: articles_bool_exp) {
  articles(where: $where) {
    __typename
    post_id
    state
    post {
      __typename
      title
      type
      slug
      created_at
    }
  }
}
